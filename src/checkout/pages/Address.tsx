import { FormAddress } from "../components/FormAddress";
import { ShopLayout } from "../../layouts";

export const Address = () => {
  return (
    <ShopLayout>
      <FormAddress />
    </ShopLayout>
  );
};
